import { ConstructibleBuilder } from "../builders";
import { ActionGroupBundle } from "../core";
/**
 * @description create building for testing
 */
export declare function createGodConstructible(actionGroupBundle: ActionGroupBundle, constructibleType?: string): ConstructibleBuilder;
